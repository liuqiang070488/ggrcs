% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggrcs.R
\name{ggrcs}
\alias{ggrcs}
\title{ggrcs}
\usage{
ggrcs(
  data,
  fit,
  x,
  histlimit = NULL,
  histbinwidth = NULL,
  histcol = NULL,
  linetype = NULL,
  linesize = NULL,
  ribalpha = NULL,
  ribcol = NULL,
  xlab = NULL,
  ylab = NULL,
  subtitle = NULL,
  caption = NULL,
  leftaxislimit = NULL,
  lift = TRUE,
  scale_y_continuous = NULL,
  title = NULL,
  ...
)
}
\arguments{
\item{data}{need a dataframe}

\item{fit}{You need the fitted model.Must be  lrm or coxph.}

\item{x}{The target variable you wish to fit. It is displayed on the X-axis when plotting.}
}
\value{
a picture
}
\description{
A function to draw histograms and restricted cubic splines (RCS).
}
\details{
You can use this function to easily draw a combined.histogram and restricted cubic spline.The function draws the graph through ggplot2.RCS fitting requires the use of the rcs function of the RMS package.Can fit cox regression,logistic regression and linear regression models.
}
\examples{
library(rms)
library(ggplot2)
library(scales)
dt<-smoke
dd<-datadist(dt)
options(datadist='dd')
fit <- cph(Surv(time,status==1) ~ rcs(age,4), x=TRUE, y=TRUE,data=dt)
ggrcs(data=dt,fit=fit,x="age")
}
